[metadata]
name = idempotency-lib
version = 1.0.0

[options]
package_dir =
    = src
packages = find:
python_requires = >=3.7
install_requires =
    boto3>=1.26.0

[options.packages.find]
where = src
